{% extends 'base.html.twig' %}

{% block title %}Foo{% endblock %}

{% block body %}
    <h1>Foo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ foo.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ foo.date ? foo.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Uuid</th>
                <td>{{ foo.uuid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('foo_index') }}">back to list</a>

    <a href="{{ path('foo_edit', {'id': foo.id}) }}">edit</a>

    {{ include('foo/_delete_form.html.twig') }}
{% endblock %}
